## Zero-to-Hero Tutorial (canonical path)

The full “build this framework from scratch” tutorial lives here:

- `documentation/ZERO_TO_HERO_TUTORIAL.md`

This file exists to provide a **stable canonical path** across the portfolio:

- `docs/ZERO_TO_HERO.md` (this file) → canonical pointer
- `documentation/ZERO_TO_HERO_TUTORIAL.md` → full tutorial content
- `docs/ZERO-TO-HERO.md` and `documentation/ZERO-TO-HERO.md` → legacy stubs (hyphen filename)

If you’re starting from zero, open `documentation/ZERO_TO_HERO_TUTORIAL.md`.

**Implementation-first reading (before trusting any doc):** `conftest.py`, `config/settings.py`, `utils/webdriver_factory.py`, `utils/diff_handler.py`, and `tests/` — those files define how the framework actually runs today.
